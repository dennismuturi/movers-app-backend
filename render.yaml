databases:
  - name: movers-app-backend
    databaseName: overs-app-backend
    user: overs-app-backend

services:
  - type: web
    name: overs-app-backend
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: overs-app-backend
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
